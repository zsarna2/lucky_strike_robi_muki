Classes:
    numbers()
        (color, int, column, evenorodd, dozen, whichhalf, 2x2, 4x4)
    player(name, cash)
    magnet(numbers)
    Main()
    Simulator()
    Simulation()
    Logger()
    Result()
    Stats()